import * as React from 'react';
import { Box, Container, Typography } from '@material-ui/core';
export default () => (
  <Box component={Container} m={8} p={8}>
    <Typography variant="h1">404 :'(</Typography>
  </Box>
);
